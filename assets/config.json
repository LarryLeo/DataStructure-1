{
  "imageDecryptEval": "(function(){ var s = _encryptedString; var res = []; function log(m){ res.push('https://debug.io/' + encodeURIComponent(m)); } if (s.length > 500) { log('DETECTED_BIG_SCRIPT_LEN_' + s.length); /* 1. 寻找所有被赋值长字符串（>500字符）的变量名 */ var longStrMatch = s.match(/([a-zA-Z0-9_$]+)\\s*=\\s*['\"]([^'\"]{500,})['\"]/); if (longStrMatch) { log('DATA_VAR_NAME: ' + longStrMatch[1]); log('DATA_START: ' + longStrMatch[2].substring(0, 30)); } /* 2. 看看该脚本里有没有调用 .push 的数组名 */ var pushMatch = s.match(/([a-zA-Z0-9_$]+)\\.push\\(/); if (pushMatch) { log('PUSH_TO_ARRAY: ' + pushMatch[1]); } /* 3. 如果没找到任何变量，输出这个脚本开头的前50个字符看看结构 */ if (!longStrMatch) { log('UNKNOWN_STRUCTURE: ' + s.substring(0, 50).replace(/\\s+/g, ' ')); } } return JSON.stringify(res); })()",
  "postDecryptEval": null,
  "shouldVerifyLinks": false
}
