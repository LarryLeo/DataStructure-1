{
  "imageDecryptEval": "(function(){ var s = _encryptedString; var results = []; function log(msg) { /* 伪装成 URL，避免 App 报错，这样你能在链接详情里看到完整信息 */ results.push('https://debug.io/' + encodeURIComponent(msg)); } try { /* 1. 探测是否有任何包含大量 Base64 特征的脚本 */ if (s.length > 500) { log('LARGE_SCRIPT_DETECTED: ' + s.length); /* 2. 这里的正则极度放宽，寻找任何 '变量 = 字符串' 的模式 */ var anyVar = s.match(/([a-zA-Z0-9_$]+)xnz\\s*=/); if (anyVar) { log('POSSIBLE_VAR: ' + anyVar[1]); } else { /* 如果没找到 xnz，看看有没有类似 _B18... 这种规律的变量 */ var patternVar = s.match(/(_[a-zA-Z0-9]{5,})\\s*=/); if (patternVar) log('MAYBE_VAR_WITHOUT_XNZ: ' + patternVar[1]); } /* 3. 看看脚本的前 100 个字符到底长啥样，排除转义问题 */ log('STR_START: ' + s.substring(0, 100).replace(/\\s+/g, ' ')); } } catch (e) { log('ERR: ' + e.message); } return JSON.stringify(results); })()",
  "postDecryptEval": null,
  "shouldVerifyLinks": false
}
