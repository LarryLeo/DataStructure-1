{
  "imageDecryptEval": "(function() { var s = _encryptedString; var log = []; function dbg(m) { log.push('https://debug.io/' + encodeURIComponent(m)); } try { dbg('LEN_' + s.length); if (s.length < 5000) return JSON.stringify(log); if (s.indexOf('Array()') === -1) { dbg('NO_ARRAY'); return JSON.stringify(log); } var header = s.substring(0, 300).replace(/\\s+/g, ' '); dbg('HEAD_' + header); var varMatch = s.match(/var\\s+c_[a-zA-Z0-9]+\\s*=\\s*new\\s*Array\\(\\);\\s*var\\s+(_[a-zA-Z0-9]+)\\s*=\\s*new\\s*Array\\(\\)/); if (!varMatch) varMatch = s.match(/var\\s+(_[a-zA-Z0-9]+)\\s*=\\s*new\\s*Array\\(\\)/); if (varMatch) { dbg('VAR_' + varMatch[1]); var varName = varMatch[1]; var escName = varName.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&'); var pattern = escName + '\\\\.push\\\\s*\\\\(\\\\s*[\\'\"` ]([^\\'\"` ]{50,})[\\'\"` ]'; var pushReg = new RegExp(pattern, 'g'); var m, count = 0; while (m = pushReg.exec(s)) { count++; if (count === 1) dbg('FIRST_' + m[1].substring(0, 50)); if (count <= 3) log.push('https://2.bp.blogspot.com/test'); } dbg('COUNT_' + count); } else { dbg('NO_VAR_MATCH'); } return JSON.stringify(log); } catch (e) { dbg('ERR_' + (e.message || 'unknown')); return JSON.stringify(log); } })()",
  "postDecryptEval": null,
  "shouldVerifyLinks": false
}
