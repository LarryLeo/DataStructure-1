{
  "imageDecryptEval": "(function() { var s = _encryptedString; var log = []; function dbg(m) { log.push('https://debug.io/' + encodeURIComponent(m)); } try { var len = s.length; if (len < 5000) { dbg('SKIP_' + len); return JSON.stringify(log); } dbg('PROC_' + len); var m1 = s.match(/var\\s+c_[a-zA-Z0-9]+\\s*=\\s*new\\s*Array\\(\\);\\s*var\\s+(_[a-zA-Z0-9]+)\\s*=\\s*new\\s*Array\\(\\)/); dbg('M1_' + (m1 ? m1[1] : 'null')); if (!m1) { var m2 = s.match(/var\\s+(_[a-zA-Z0-9]+)\\s*=\\s*new\\s*Array\\(\\)/); dbg('M2_' + (m2 ? m2[1] : 'null')); if (m2) m1 = m2; } if (m1) { var vn = m1[1]; dbg('USE_' + vn); var esc = vn.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&'); var pat = esc + '\\\\.push\\\\s*\\\\(\\\\s*[\\'\"` ]([^\\'\"` ]{50,})[\\'\"` ]'; var rx = new RegExp(pat, 'g'); var cnt = 0; var mx; while (mx = rx.exec(s)) { cnt++; if (cnt <= 2) log.push('https://2.bp.blogspot.com/test' + cnt); } dbg('FOUND_' + cnt); } return JSON.stringify(log); } catch (e) { dbg('ERR_' + String(e)); return JSON.stringify(log); } })()",
  "postDecryptEval": null,
  "shouldVerifyLinks": false
}
