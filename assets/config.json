{
  "imageDecryptEval": "(function(){ var s = _encryptedString; var res = []; function log(m){ res.push('https://debug.io/' + encodeURIComponent(m)); } if (s.length > 20000) { log('BIG_ONE_' + s.length); var regex = /([a-zA-Z0-9_$]+)\\s*(\\+?=)\\s*['\"`]([^'\"`]{1000,})['\"`]/g; var m; while ((m = regex.exec(s)) !== null) { log('VAR:' + m[1] + ' OP:' + m[2] + ' DATA:' + m[3].substring(0, 50)); } var pushRegex = /([a-zA-Z0-9_$]+)\\.push\\(['\"`]([^'\"`]{1000,})['\"`]\\)/g; while ((m = pushRegex.exec(s)) !== null) { log('PUSH:' + m[1] + ' DATA:' + m[2].substring(0, 50)); } } return JSON.stringify(res); })()",
  "postDecryptEval": null,
  "shouldVerifyLinks": false
}
