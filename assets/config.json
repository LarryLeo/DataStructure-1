{
  "imageDecryptEval": "(function() { var s = _encryptedString; var log = []; function dbg(m) { log.push('https://debug.io/' + encodeURIComponent(m)); } try { if (s.length < 20000) { dbg('SKIP_LEN_' + s.length); return JSON.stringify(log); } dbg('LEN_' + s.length); var varMatch = s.match(/var\\s+c_[a-zA-Z0-9]+\\s*=\\s*new\\s*Array\\(\\);\\s*var\\s+(_[a-zA-Z0-9]+)\\s*=\\s*new\\s*Array\\(\\)/); if (!varMatch) varMatch = s.match(/var\\s+(_[a-zA-Z0-9]+)\\s*=\\s*new\\s*Array\\(\\)/); if (varMatch) { dbg('VAR_' + varMatch[1]); var varName = varMatch[1]; var escName = varName.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&'); dbg('ESC_' + escName); var pattern = escName + '\\\\.push\\\\s*\\\\(\\\\s*[\\'\"` ]([^\\'\"` ]{50,})[\\'\"` ]'; dbg('PAT_' + pattern.substring(0, 80)); var pushReg = new RegExp(pattern, 'g'); var m, count = 0; while (m = pushReg.exec(s)) { count++; if (count === 1) dbg('FIRST_' + m[1].substring(0, 80)); if (count <= 3) log.push('https://2.bp.blogspot.com/test'); } dbg('COUNT_' + count); } else { dbg('NO_VAR'); } return JSON.stringify(log); } catch (e) { dbg('ERR_' + e.message); return JSON.stringify(log); } })()",
  "postDecryptEval": null,
  "shouldVerifyLinks": false
}
